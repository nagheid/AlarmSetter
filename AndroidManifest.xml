<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cutc.alarmsetter"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <!--
    <uses-permission android:name="android.permission.INTERNET"> 
    </uses-permission>
    -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.cutc.alarmsetter.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.cutc.alarmsetter.ParametersEditor"
            android:label="@string/title_activity_parameters_editor" >
        </activity>
        <activity
            android:name="com.cutc.alarmsetter.AlarmSetter"
            android:label="@string/app_name" >

            <!-- FIXTHIS -->
        </activity>
        <activity
            android:name="com.cutc.alarmsetter.AlarmCreator"
            android:label="@string/title_activity_alarm_creator" >
        </activity>
        <activity
            android:name="com.cutc.alarmsetter.Temp"
            android:label="@string/title_activity_temp" >
        </activity>
        <activity
            android:name="com.cutc.alarmsetter.Preferences"
            android:label="@string/title_activity_preferences" >
        </activity>
    </application>

</manifest>